/**
 * 字符串字面量类型
 * 用来约束取值只能是某几个字符串中的一个
 * 类型别名与字符串字面量类型都是使用 type 进行定义
 */

/*
  type FriendsName = 'dxw' | 'da' | 'hr';
  function getName(name: FriendsName): string {
    return name;
  }
  console.log(getName('dxw'));
 */

/*
  type arr = 1 | 2 | '3';
  const arg: arr = 2;
 */
